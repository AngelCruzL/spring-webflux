version: '3'

services:
  db_service:
    container_name: springbootWebfluxDB
    image: mongo:5
    restart: always
    ports:
      - "27017:27017"
    environment:
      MONGODB_DATABASE: springboot_webflux_db
    volumes:
      - ./mongo:/data/db